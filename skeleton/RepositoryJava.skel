package @JAVA_PACKAGE@;

import org.springframework.data.mongodb.repository.MongoRepository;
import org.springframework.stereotype.Repository;

@Repository
public interface @FILENAME@ extends MongoRepository<,String> {
}
